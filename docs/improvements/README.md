# Improvements

`docs/improvements/` records proposed changes separately from the current architecture.

Lifecycle:

`IDEA → PLANNED → IN PROGRESS → IMPLEMENTED → TESTED → ACCEPTED`

An improvement becomes part of the architecture only after implementation and validation. The architecture guide must not silently describe planned work as current behavior.